The MultiLeafSkeleton.app contains the skeleton to produce the MultiLeaf Mac application bundle.

To flesh it out it needs:

Everything in the staging directory to be inserted into the:
   MultiLeafSkeleton/Contents/Resources/Java directory

